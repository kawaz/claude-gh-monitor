{
  "name": "gh-monitor",
  "owner": {
    "name": "kawaz"
  },
  "metadata": {
    "description": "Monitor GitHub asynchronous events (PR state changes, GitHub Actions workflow runs) via Claude Code hooks + Monitor tool.",
    "version": "0.3.4"
  },
  "plugins": [
    {
      "name": "gh-monitor",
      "source": "./",
      "description": "GitHub asynchronous event monitor — emits a line on each PR / CI / workflow run change so Claude Code can notify you without polling."
    }
  ]
}
